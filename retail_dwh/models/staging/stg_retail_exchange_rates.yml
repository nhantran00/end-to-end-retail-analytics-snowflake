version: 2

sources:
  - name: raw
    database: retail_db
    schema: raw
    tables:
      - name: exchange_rates
        columns:
          - name: currency
            tests:
              - not_null