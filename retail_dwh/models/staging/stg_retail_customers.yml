version: 2

sources:
  - name: raw
    database: retail_db
    schema: raw
    tables:
      - name: customers
        columns:
          - name: customer_key
            tests:
              - unique
              - not_null
