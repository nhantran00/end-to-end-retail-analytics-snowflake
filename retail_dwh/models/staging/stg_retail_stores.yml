version: 2

sources:
  - name: raw
    database: retail_db
    schema: raw
    tables:
      - name: stores
        columns:
          - name: store_key
            tests:
              - unique
              - not_null